#include <stdio.h>

/**
 * main - Entry poin
 * Discription - print numbers in baseten from zero
 * Return: 0 (Success)
 */

int main(void)
{
	int digit;

	for (digit = 0; digit <= 9; digit++)
		printf("%d", digit);
	printf("\n");
	return (0);
}
